[project]
name = "rag-workshop-stage2"
version = "0.1.0"
description = "Stage 2 — Bedrock Knowledge Base (managed RAG)"
requires-python = ">=3.10"
dependencies = [
    "boto3>=1.38.0",
    "botocore[crt]>=1.38.0",
    "python-dotenv>=1.0.0",
    "rich>=13.7.0",
]

[tool.uv]
package = false
